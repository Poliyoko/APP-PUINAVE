# SPT-019.1 — Manual operativo

El instalador ejecuta automáticamente py_compile, pytest, generación del
registro institucional y una simulación basada en archivo.

El resultado de la simulación debe indicar:

- `simulation: true`
- `executed: false`
- `result: accepted_for_simulation`